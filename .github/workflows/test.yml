name: "test-local"
on:
  pull_request:
  push:
    branches:
      - master
      - 'releases/*'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: octokit/request-action@v1
      with:
        route: POST /graphql
        query: >
          query {
            user(login: "imjohnbo"){
              name
            }
          }
